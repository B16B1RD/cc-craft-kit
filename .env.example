# GitHub Personal Access Token
# ⚠️ 個人アカウントで Projects v2 を使用する場合は Classic Personal Access Token が必須
# Fine-grained PAT は Organization の Projects のみ対応
#
# Classic PAT (個人アカウント向け):
#   GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
#   スコープ: repo, project
#
# Fine-grained PAT (Organization のみ):
#   GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens
#   Permissions: Repository (Contents, Issues, PRs), Projects
GITHUB_TOKEN=

# GitHub組織名・ユーザー名
GITHUB_OWNER=

# デフォルトプロジェクト名(オプション)
GITHUB_PROJECT_NAME=

# データベースパス(デフォルト: .cc-craft-kit/cc-craft-kit.db)
# DATABASE_PATH=

# ログレベル (debug, info, warn, error)
LOG_LEVEL=info

# MCPサーバーポート(オプション、デフォルト: stdio)
# MCP_PORT=3000
