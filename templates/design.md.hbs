# 設計書: {{specName}}

**作成日時:** {{formatDate createdAt}}

---

## 📋 概要

{{description}}

## 🏗️ アーキテクチャ

{{architecture}}

## 🧩 コンポーネント設計

{{#each components}}
### {{inc @index}}. {{this.name}}

**責務:** {{this.responsibility}}

**インターフェース:**
{{#each this.interfaces}}
- `{{this}}`
{{/each}}

{{/each}}

## 📊 データモデル

{{dataModel}}

## 🔌 APIエンドポイント

{{#if apiEndpoints}}
| メソッド | パス | 説明 |
|---------|------|------|
{{#each apiEndpoints}}
| {{this.method}} | `{{this.path}}` | {{this.description}} |
{{/each}}
{{else}}
APIエンドポイントは定義されていません。
{{/if}}

## 🔒 セキュリティ考慮事項

{{#if securityConsiderations}}
{{#each securityConsiderations}}
- {{this}}
{{/each}}
{{else}}
特別なセキュリティ考慮事項はありません。
{{/if}}

## ⚡ パフォーマンス考慮事項

{{#if performanceConsiderations}}
{{#each performanceConsiderations}}
- {{this}}
{{/each}}
{{else}}
特別なパフォーマンス考慮事項はありません。
{{/if}}

---

## 📝 備考

- この設計書は、仕様駆動開発(SDD)のDesignフェーズで作成されました。
- 前のフェーズ: **Requirements** (要件定義フェーズ)
- 次のフェーズ: **Tasks** (タスク分解フェーズ)
- 設計変更が必要な場合は、この文書を更新し、承認プロセスを経てください。

---

**Takumi (匠)** で生成 - 設計書テンプレート v1.0
